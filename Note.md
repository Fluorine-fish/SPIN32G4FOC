##  代码原则
### 1.编写原则
1. 代码即注释，注释的作用是在讲每一块的代码完成的工作；函数和变量名清晰可以实现自解释；
2. 代码命名格式统一：当你选择了一种命名方式之后，整个工程的/单个文件的命名规范必须要保持一致；
3. 高内聚低耦合：每一个功能应该是一个独立的函数，每一个函数的功能只能有一个；函数与函数尽量没有耦合关系，不要多个函数操作同一个全局变量

### 2.匈牙利命名法
#### 2.1 变量命名
前缀代表变量的类型，其余部分用英文或者缩写来表示变量作用，首字母大写
> i16Cnt 可以用来表示有符号16位的整形计数值

#### 2.2 宏定义
宏定义全部大写，变量如果涉及计算则需要加括号；所有常数建议宏定义，代码中不要有魔法数；

### 2.3 函数命名规范
1. 首字母大写单词来对函数命名
2. 全大写前缀可以用来做模块区分
